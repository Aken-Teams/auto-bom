# Auto-BOM 系统 — 待确认问题清单

以下问题需要李海东确认后，系统才能进一步完善。

---

## 问题 1：罐头对照表不完整

目前罐头对照表只有 **18 个 WAF 对应关系**，但料号清单中有些原件找不到对应的罐头：

- `WAF108612_CP` → 无对应
- `WAF105220_CP` → 无对应
- `WAF110786_CP` → 无对应
- ... 等多个

**请确认：**
- 找不到罐头对照的料号该如何处理？
  - A) 由 User 手动在系统中选择/输入罐头代码？
  - B) 按某个规则自动 fallback（例如按 mil 数匹配最接近的）？
  - C) 需要补齐罐头对照表？

---

## 问题 2：成型罐头和包装罐头的默认值

罐头对照表 18 笔记录中，只有 **1 笔**同时有焊接+成型+包装三种罐头，其他 17 笔只有焊接罐头。

但从 `pj_bom_loader` 的输出来看，每个料号都需要成型罐头和包装罐头。

**请确认：**
- 成型罐头是否有固定默认值？（目前看起来大多是 `SMC_MD0015`）
- 包装罐头的选择规则是什么？
  - 目前看到两种：`SMC_PA0009`（TR 7"/0.8K）和 `SMC_PA0011`（TR 13"/3K）
  - 判断依据是料号的包装规格（7寸 vs 13寸）吗？
  - 还是由 User 手动指定？

---

## 问题 3：BOM 底稿和导入清单料号的关系

分析发现 BOM 底稿（WXBMR005）和导入清单（C-CMAX）的料号 **完全不重叠**：
- 底稿有 21 个料号（DO-201AE/TVS 系列，如 `1.5KE100A`）
- 清单有 174 个料号（SMC 系列，如 `MURC5J`, `SK52P`, `ER3A`）

**请确认：**
- 底稿是否只是给我们参考格式用的？实际用的底稿料号会是 SMC 相关的？
- 还是说底稿和清单本来就是不同产品线，流程中底稿的作用是什么？
- 系统从底稿中"筛选料号"这一步，具体的筛选逻辑是什么？

---

## 问题 4：替代结构命名规则

导入清单中的 MAX 替代结构格式为 `ACP_xxM`（如 `ACP_70M`），其中 `xx` = mil 数。

**请确认：**
- 这个命名规则是固定的吗？系统可以根据 mil 数自动生成替代结构名称？
- 还是有其他命名规则需要遵循？

---

## 问题 5：标准作业 ID 的匹配逻辑

`sequences-raw` 中的 `STANDARD_OPERATION_ID` 需要从 `WXBMR004_标准作业` 中查找。

**请确认：**
- 标准作业 ID 的匹配规则是什么？
  - 按站别（切割/焊接/成型/包装）+ 产品 FAMILY 来决定？
  - 还是按其他字段组合查找？
- `sequences-raw` 中的站别序号（10/20/30/40）分别对应什么标准作业？

---

## 问题 6：同一料号多个原件（替代料）的处理

从底稿来看，一个料号（如 `1.5KE100A_00001`）可能对应多个原件（多个 WAF 或 CHP）。

**请确认：**
- 导入清单中也会出现一对多的情况吗？（已看到 `S5B_R2_00001` 对应 4 个不同原件）
- 多个原件的情况下，罐头是根据每个原件分别配？还是取其中一个？
- 输出的 `pj_bom_loader` 中，每个原件都要生成一条记录吗？

---

## 问题 7：输出文件的完整字段

目前 MVP 只生成了 3 个输出文件的核心字段。

**请确认：**
- `pj_bom_loader` 除了核心 7 个字段外，Col 17-23（BOP, PROCESS_SPEC, BOM_NAME, CHIP_QTY, WIR_QTY, DB SEQUENCE, COMPONENT_ALTERNATE_QTY）是否也需要填写？规则是什么？
- `routings` 的 60 个字段中，除了 ALTERNATE_ROUTING_DESIGNATOR 和 ROUTING_TYPE，还有哪些是必填的？
- `sequences-raw` 的 99 个字段中，必填字段有哪些？

---

*以上问题回答后，系统将进行相应的逻辑调整和完善。*
